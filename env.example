# Environment variables for 3DCF/doc2dataset and evaluation
# IMPORTANT:
# - Do NOT commit this file to git.
# - Copy it to `.env` and fill in your real keys locally.
# - Never share the filled-in `.env` file

HF_TOKEN=hf_***
OPENAI_API_KEY=sk-***
ANTHROPIC_API_KEY=sk-***

# Optional: which provider/model doc2dataset should use for QA/summary/RAG tasks
DOC2DATASET_PROVIDER=openai          # or anthropic, local, etc.
DOC2DATASET_MODEL=gpt-4.1-mini
DOC2DATASET_LANG=en

# Optional: evaluation-specific variables (if you use them in eval scripts)
EVAL_PROVIDER=openai
EVAL_OPENAI_MODEL=gpt-4.1-mini